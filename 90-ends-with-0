public Boolean isEndWithZero(Integer num){
     //code here
     Integer remainder = Math.mod(num, 2);

        if (remainder == 0) {
            return true;
        } else {
            return false;
        }
        
}