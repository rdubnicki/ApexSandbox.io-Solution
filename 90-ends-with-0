public Boolean isEndWithZero(Integer num){
     //code here
     Integer remainder = Math.mod(num, 1);

        if (remainder == 0) {
            return true;
        } else if (remainder != 0) {
            return false;
        }
}